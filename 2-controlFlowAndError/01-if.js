/**************
 * ? If Conditional
 *********** */


let isOn = true;

if (isOn == true) {
    console.log('The light is on!')
};

// looks for keyword, then condition you want to check. If meets condition, open set of curly braces. 
// if (condition ) {
//      Console log ( )
// };

if (isOn) {
    console.log('The light is on, yay!')
}

let isOff = false;

if (isOff == false) {
    console.log('The light is off!');
}

let weather = 65;

if (weather < 70 ) {
    console.log('Wear a jacket!');
};